import React from "react";
import { createContext } from "react";
//Cart array contains all of the products
//Add to cart button will check if cart has item already - if yes then increment the item quantity in cart and if not then oush new item into cart.
//Also should have a funtion that checks if product is in stock or not

